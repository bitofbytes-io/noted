server {
    listen 80;
    listen [::]:80;

    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    location = /health {
        access_log off;
        add_header Content-Type application/json;
        return 200 '{"status":"ok"}';
    }

    location / {
        try_files $uri $uri/ /index.html;
    }

    # Both PDF.js and alphaTab load ES module workers. The stock image does not
    # map .mjs consistently, and nosniff correctly rejects octet-stream modules.
    location ~* \.mjs$ {
        try_files $uri =404;
        default_type application/javascript;
    }

    add_header X-Content-Type-Options nosniff always;
    add_header Referrer-Policy same-origin always;
}
